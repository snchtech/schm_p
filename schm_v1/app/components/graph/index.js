import RectElement from './Rect';
import CircleElement from './Circle';
import TextElement from './TextElement';
import LineElement from './LineElement'; // Виправлено пробіл перед шляхом
import Symbol1 from './Symbol1'; // Виправлено пробіл перед шляхом

const GraphLibrary = {
    rect: RectElement,
    circle: CircleElement,
    text: TextElement,
    line: LineElement,
    symbol1: Symbol1,
};

export default GraphLibrary;